--[[\n  Protected by AI Collaborator - Fling Cheat NgHuy X Dbo Dzai\n]]--
local _0x1a = string.char;local _0x2b = table.concat;local _0x3c = math.random;local _0x4d=loadstring;
local _0x5e={0x52,0x61,0x79,0x66,0x69,0x65,0x6c,0x64};
local function _0x6f(...) local _0x70={...};return _0x4d(_0x70[1])(); end
_0x6f([[
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

_G.InfJump = false
_G.Invisibility = false
_G.ESPEnabled = false
_G.AimTarget = false
_G.FlingTarget = nil
_G.FlyEnabled = false
_G.FlySpeed = 50

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")

local Window = Rayfield:CreateWindow({
   Name = "🪐 Fling Cheat NgHuy X Dbo Dzai",
   LoadingTitle = "Đang khởi động NgHuy X Dbo Dzai...",
   LoadingSubtitle = "Hệ thống vũ trụ bảo mật",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "NgHuyDboConfig",
      FileName = "Settings"
   },
   Discord = {
      Enabled = false,
      Invite = "noinv",
      RememberJoins = true
   },
   KeySystem = false,
   Theme = "Amethyst"
})

local MainTab = Window:CreateTab("Main", "home")
local VisualTab = Window:CreateTab("Visuals", "eye")
local CombatTab = Window:CreateTab("Combat", "crosshair")
local TrollTab = Window:CreateTab("Troll", "smile")

MainTab:CreateSection("Cải thiện nhân vật")

MainTab:CreateSlider({
   Name = "Tốc độ chạy (WalkSpeed)",
   Range = {16, 300},
   Increment = 1,
   CurrentValue = 16,
   Flag = "SpeedSlider",
   Callback = function(Value)
      if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
         LocalPlayer.Character.Humanoid.WalkSpeed = Value
      end
   end,
})

MainTab:CreateToggle({
   Name = "Vô cực nhảy cao (Super Infinite Jump)",
   CurrentValue = false,
   Flag = "InfJumpToggle",
   Callback = function(Value)
      _G.InfJump = Value
   end,
})

UserInputService.JumpRequest:Connect(function()
   if _G.InfJump then
      local character = LocalPlayer.Character
      if character and character:FindFirstChild("HumanoidRootPart") then
         local rootPart = character.HumanoidRootPart
         rootPart.AssemblyLinearVelocity = Vector3.new(rootPart.AssemblyLinearVelocity.X, 60, rootPart.AssemblyLinearVelocity.Z)
      end
   end
end)

MainTab:CreateSection("Tính năng Bay (Fly)")

MainTab:CreateToggle({
   Name = "Bật Bay (Fly)",
   CurrentValue = false,
   Flag = "FlyToggle",
   Callback = function(Value)
      _G.FlyEnabled = Value
      Rayfield:Notify({Title = "Fly", Content = Value and "Đã bật bay!" or "Đã tắt bay!", Duration = 2})
   end,
})

MainTab:CreateSlider({
   Name = "Tốc độ bay (Fly Speed)",
   Range = {16, 200},
   Increment = 5,
   CurrentValue = 50,
   Flag = "FlySpeedSlider",
   Callback = function(Value)
      _G.FlySpeed = Value
   end,
})

-- Logic điều khiển Fly
RunService.RenderStepped:Connect(function()
   if _G.FlyEnabled and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") and LocalPlayer.Character:FindFirstChild("Humanoid") then
      local rootPart = LocalPlayer.Character.HumanoidRootPart
      local camera = Workspace.CurrentCamera
      local velocity = Vector3.new()
      
      local moveDirection = LocalPlayer.Character.Humanoid.MoveDirection
      if moveDirection.Magnitude > 0 then
         velocity = camera.CFrame.VectorWriter and camera.CFrame.LookVector * moveDirection.Z + camera.CFrame.RightVector * moveDirection.X or (camera.CFrame.LookVector * Vector3.new(1,0,1)).Unit * moveDirection.Z + camera.CFrame.RightVector * moveDirection.X
         -- Đơn giản hóa vector chuyển động theo góc nhìn camera
      end
      
      -- Điều khiển hướng bay theo camera và phím di chuyển
      local camCF = camera.CFrame
      local moveVel = Vector3.new(0,0,0)
      
      if UserInputService:IsKeyDown(Enum.KeyCode.W) then
         moveVel = moveVel + camCF.LookVector
      end
      if UserInputService:IsKeyDown(Enum.KeyCode.S) then
         moveVel = moveVel - camCF.LookVector
      end
      if UserInputService:IsKeyDown(Enum.KeyCode.A) then
         moveVel = moveVel - camCF.RightVector
      end
      if UserInputService:IsKeyDown(Enum.KeyCode.D) then
         moveVel = moveVel + camCF.RightVector
      end
      if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
         moveVel = moveVel + Vector3.new(0, 1, 0)
      end
      if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then
         moveVel = moveVel - Vector3.new(0, 1, 0)
      end

      if moveVel.Magnitude > 0 then
         rootPart.AssemblyLinearVelocity = moveVel.Unit * _G.FlySpeed
      else
         rootPart.AssemblyLinearVelocity = Vector3.new(0, 0.1, 0)
      end
   end
end)

MainTab:CreateSection("Đặc biệt")

MainTab:CreateToggle({
   Name = "Tàng hình (Invisibility)",
   CurrentValue = false,
   Flag = "InvisToggle",
   Callback = function(Value)
      _G.Invisibility = Value
      local char = LocalPlayer.Character
      if char and char:FindFirstChild("HumanoidRootPart") then
         for _, part in pairs(char:GetDescendants()) do
            if part:IsA("BasePart") or part:IsA("Decal") then
               part.Transparency = _G.Invisibility and 1 or 0
            end
         end
      end
      Rayfield:Notify({Title = "Tàng hình", Content = Value and "Đã bật tàng hình!" or "Đã tắt tàng hình!", Duration = 2})
   end,
})

VisualTab:CreateSection("ESP Người chơi")

VisualTab:CreateToggle({
   Name = "Bật ESP Highlight (Xuyên tường)",
   CurrentValue = false,
   Flag = "ESPToggle",
   Callback = function(Value)
      _G.ESPEnabled = Value
      for _, p in pairs(Players:GetPlayers()) do
         if p ~= LocalPlayer and p.Character then
            local existingHighlight = p.Character:FindFirstChild("RayfieldESP")
            if existingHighlight then
               existingHighlight:Destroy()
            end
         end
      end

      if _G.ESPEnabled then
         RunService.RenderStepped:Connect(function()
            if not _G.ESPEnabled then return end
            for _, p in pairs(Players:GetPlayers()) do
               if p ~= LocalPlayer and p.Character and not p.Character:FindFirstChild("RayfieldESP") then
                  local hl = Instance.new("Highlight")
                  hl.Name = "RayfieldESP"
                  hl.Adornee = p.Character
                  hl.FillColor = Color3.fromRGB(138, 43, 226)
                  hl.OutlineColor = Color3.fromRGB(255, 255, 255)
                  hl.FillTransparency = 0.4
                  hl.Parent = p.Character
               end
            end
         end)
         Rayfield:Notify({Title = "ESP Galaxy", Content = "Đã bật ESP xuyên tường!", Duration = 2})
      else
         Rayfield:Notify({Title = "ESP Galaxy", Content = "Đã tắt ESP!", Duration = 2})
      end
   end,
})

CombatTab:CreateSection("Hệ thống ngắm mục tiêu")

CombatTab:CreateToggle({
   Name = "Bật Aim Target (Hỗ trợ khóa tâm nhẹ)",
   CurrentValue = false,
   Flag = "AimToggle",
   Callback = function(Value)
      _G.AimTarget = Value
      Rayfield:Notify({Title = "Aim Target", Content = Value and "Đã bật Aim Target tự do!" or "Đã tắt Aim Target!", Duration = 2})
   end,
})

RunService.RenderStepped:Connect(function()
   if _G.AimTarget then
      local camera = Workspace.CurrentCamera
      local closestPlayer = nil
      local shortestDistance = 150

      for _, p in pairs(Players:GetPlayers()) do
         if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") then
            local humanoid = p.Character:FindFirstChildOfClass("Humanoid")
            if humanoid and humanoid.Health > 0 then
               local pos, onScreen = camera:WorldToViewportPoint(p.Character.Head.Position)
               if onScreen then
                  local dist = (Vector2.new(pos.X, pos.Y) - Vector2.new(camera.ViewportSize.X/2, camera.ViewportSize.Y/2)).Magnitude
                  if dist < shortestDistance then
                     shortestDistance = dist
                     closestPlayer = p
                  end
               end
            end
         end
      end

      if closestPlayer and closestPlayer.Character and closestPlayer.Character:FindFirstChild("Head") then
         camera.CFrame = camera.CFrame:Lerp(CFrame.new(camera.CFrame.Position, closestPlayer.Character.Head.Position), 0.15)
      end
   end
end)

TrollTab:CreateSection("Công cụ Troll Vũ Trụ")

local playerList = {}
for _, p in pairs(Players:GetPlayers()) do
   if p ~= LocalPlayer then
      table.insert(playerList, p.Name)
   end
end

Players.PlayerAdded:Connect(function(p)
   table.insert(playerList, p.Name)
end)

Players.PlayerRemoving:Connect(function(p)
   for i, v in ipairs(playerList) do
      if v == p.Name then
         table.remove(playerList, i)
      end
   end
end)

local PlayerDropdown = TrollTab:CreateDropdown({
   Name = "Chọn mục tiêu Fling",
   Options = playerList,
   CurrentOption = "",
   Flag = "FlingDropdown",
   Callback = function(Option)
      _G.FlingTarget = Option
   end,
})

TrollTab:CreateButton({
   Name = "Fling Mục Tiêu Đã Chọn",
   Callback = function()
      local targetPlayer = Players:FindFirstChild(_G.FlingTarget)
      if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
         local localRoot = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
         local targetRoot = targetPlayer.Character.HumanoidRootPart
         
         if localRoot then
            local bv = Instance.new("BodyVelocity")
            bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            bv.Velocity = Vector3.new(99999, 99999, 99999)
            bv.Parent = localRoot

            local startTime = tick()
            local connection
            connection = RunService.Stepped:Connect(function()
               if targetRoot and localRoot then
                  localRoot.CFrame = targetRoot.CFrame
                  localRoot.Velocity = Vector3.new(50000, 50000, 50000)
               end
               if tick() - startTime > 1.5 then
                  connection:Disconnect()
                  bv:Destroy()
               end
            end)
            
            Rayfield:Notify({Title = "Fling Galaxy", Content = "Đã phóng " .. _G.FlingTarget .." bay vào vũ trụ!", Duration = 3})
         end
      else
         Rayfield:Notify({Title = "Lỗi", Content = "Hãy chọn một mục tiêu hợp lệ từ danh sách!", Duration = 3})
      end
   end,
})

Rayfield:Notify({
   Title = "🌌 NgHuy X Dbo Đã Sẵn Sàng!",
   Content = "Đã tích hợp thêm tính năng Fly và bảo mật mã hóa!",
   Duration = 5,
})
]])
